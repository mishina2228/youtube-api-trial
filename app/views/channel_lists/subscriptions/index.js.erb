$('#search-result')
  .html("<%= j(render 'channels', subscription: @subscription) %>");
$('#search-result-pagination')
  .html("<%= j(render 'channels_pagination', subscription: @subscription, condition: @condition) %>")
  .trigger('ajax:success')
propSubmitButton();
