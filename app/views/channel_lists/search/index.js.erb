$('#search-result')
  .html("<%= j(render 'channels', search_results: @search_results) %>");
$('#search-result-pagination')
  .html("<%= j(render 'channels_pagination', search_results: @search_results, condition: @condition) %>");
prop_submit_button();
