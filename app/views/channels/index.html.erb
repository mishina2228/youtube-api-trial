<h1>
  <%= t('helpers.title.list', models: Channel.model_name.human.pluralize(I18n.locale)) %>
</h1>

<%= render partial: 'actions' %>

<div id="search-result-pagination">
  <%= render partial: 'search_result', locals: {channels: @channels} %>
</div>
