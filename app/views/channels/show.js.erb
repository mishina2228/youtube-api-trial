{
  const statistics = document.getElementById('search-statistics')
  statistics.innerHTML = "<%= j(render 'statistics', channel_statistics: @channel_statistics ) %>"
  statistics.dispatchEvent(new window.Event('ajax:success'))
}
